{
  type Text = string;
  const name: Text = "Sangji";

  type Num = number;
  const score: Num = 10;

  type Student = {
    name: string;
    age: number;
  };

  const student: Student = {
    name: "sangji",
    age: 27,
  };

  type Bom = "Sangji's cat";
  let bom: Bom;
  // bom = "Someone's cat";
}
