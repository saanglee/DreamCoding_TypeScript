"use strict";
{
    // Array
    const fruits = ['🍅', '🍌'];
    const scroes = [1, 3, 4];
    function printArray(fruits) { }
    // Tuple -> interface, type alias, class
    let student;
    student = ['name', 123];
    student[0]; // name
    student[1]; // 123
    const [name, age] = student;
}
